# 個股每日資訊網站 — 實作規劃

> 目標：把夥伴每天整理的 Excel（大盤放量訊號 + 個股分點/圖表）轉成一個**公開網站**，
> 夥伴照舊產出 Excel 丟進資料夾，你跑一次程式網站就更新。

---

## 一、整體架構（建議：靜態網站 + 轉檔程式）

```
夥伴每天產出 Excel
        │  （丟進 data/YYYYMMDD/）
        ▼
┌─────────────────────┐
│  build.py 轉檔程式   │  ← 你每天跑一次
│  Excel → JSON + PNG  │
└─────────────────────┘
        │
        ▼
┌─────────────────────┐
│   site/ 靜態網站     │  ← HTML + JS + 資料檔
│   (純前端，無後端)   │
└─────────────────────┘
        │  上傳
        ▼
   GitHub Pages / Netlify / Cloudflare Pages（公開、免費）
```

**為什麼選「靜態網站」而不是把 Excel 直接放上網或架後端？**

- 原始 Excel 單檔上萬列、整天份上百 MB，瀏覽器直接讀會很慢；轉成精簡 JSON 後只留要顯示的欄位，載入快。
- 公開網站若用靜態架構，沒有伺服器、沒有資料庫，**幾乎零維護、零成本、也較安全**（沒有後端可被攻擊）。
- 部署簡單：把 `site/` 資料夾推上 GitHub Pages 即可，網址固定。

---

## 二、資料來源對照（已掃描現有兩天資料）

| 來源檔案 | 分頁 | 對應網頁區塊 |
|---|---|---|
| `台股放量訊號_YYYYMMDD.xlsx` → `今日放量訊號` | 約 1000 列、23 欄 | **大盤放量訊號**（首頁主表，可排序/篩選） |
| 同上 → `勝率回測` | 22 列 | **勝率回測統計**（星等 / 量價 / 市場分組） |
| 同上 → `量能延續追蹤`、`出關股追蹤`、`處置股清單` | — | 大盤頁次要分頁 |
| `{代號}_{日期}_日報.xlsx` → `買進前20`、`賣出前20` | 各 20 列 | **個股券商分點**（買賣超前 20 大券商） |
| `{代號}_{日期}分析結果含表百分比.xlsx` → `買賣價量與家數` | 約 16 列 | 個股「各價位買賣量」表/圖 |
| 同上 → `券商明細` | 約 1.2 萬列 | 個股分點明細（建議做篩選，不全列） |
| `{代號}_{日期}_charts.xlsx` | 內嵌 6 張 PNG | **個股圖表**（技術線圖） |

目前個股：2344、3037、3105、6257、6261（程式會自動偵測，新增個股不用改程式）。

---

## 三、網站頁面設計

**1. 首頁 / 大盤總覽**（對應「大盤放量訊號」）
- 頂部：日期選單（自動列出所有有資料的日期）。
- 主表：當日放量訊號約 1000 檔，欄位含代號、名稱、訊號強度（★）、當日量、漲跌幅、量價關係、各倍數。
- 互動：依任一欄排序、依星等/市場/量價關係篩選、關鍵字搜尋代號或名稱。

**2. 勝率回測頁**（對應「勝率回測」）
- 把星等、量價關係、訊號類型、市場等分組的 D+5 / D+10 / D+15 勝率與均報酬做成表格 + 長條圖。

**3. 個股詳情頁**（點主表任一檔或從個股清單進入）
- 上方：6 張技術圖表（PNG）。
- 中段：買進前 20 / 賣出前 20 券商分點。
- 下段：各價位買賣量與家數表 + 圖；券商明細（可搜尋券商、依占比排序）。

**4. 全站**：日期切換時所有頁面同步該日資料；手機/桌機皆可看（RWD）。

---

## 四、轉檔程式（build.py）做的事

1. 掃描 `data/` 下所有 `YYYYMMDD` 資料夾。
2. 每個放量訊號檔 → 抽出需要的分頁存成 `site/data/YYYYMMDD/market.json`。
3. 每檔個股的三個 Excel → 合併成 `site/data/YYYYMMDD/{代號}.json`。
4. charts.xlsx 內的 PNG → 解壓到 `site/data/YYYYMMDD/charts/{代號}_N.png`。
5. 產生 `site/data/index.json`（有哪些日期、各日有哪些個股），前端靠它建選單。

**日常流程**：夥伴把當天 Excel 放進新的 `data/20260603/` → 你執行 `python build.py` → 上傳 `site/` → 完成。

---

## 五、技術選型

| 項目 | 建議 | 理由 |
|---|---|---|
| 轉檔 | Python + openpyxl | 已驗證能正確讀出所有分頁與圖片 |
| 前端 | 純 HTML + JavaScript（不需框架）| 靜態、好部署、好維護 |
| 表格互動 | Grid.js 或 DataTables | 排序/篩選/搜尋現成可用 |
| 圖表 | Chart.js | 勝率回測長條圖 |
| 部署 | GitHub Pages（或 Netlify / Cloudflare Pages）| 公開、免費、綁定固定網址 |

---

## 六、需要你確認 / 注意的事項

1. **資料公開性**：券商分點與放量訊號要放在「任何人都看得到」的公開網址。若這屬於內部研究資料，建議改成「團隊內部」存取（加密碼或內網），請先確認可公開再上線。
2. **每日更新方式**：先採「手動跑一次 + 手動上傳」。若日後想全自動（夥伴丟檔就自動更新上線），可再加排程。
3. **券商明細顯示量**：單檔近 1.2 萬列，建議網頁預設只顯示前 N 名 + 提供搜尋，避免一次塞太多。

---

## 七、建議執行順序

1. 寫 `build.py` 轉檔程式，先把現有兩天資料轉成 JSON / PNG。
2. 做首頁大盤總覽（排序/篩選）+ 日期選單。
3. 做個股詳情頁（圖表 + 分點）。
4. 做勝率回測頁。
5. 本機驗證無誤 → 部署到 GitHub Pages → 給網址。

---

*確認這個方向後，我就可以直接動手實作；若有想調整的（頁面、欄位、部署平台、公開或內部），告訴我即可。*
